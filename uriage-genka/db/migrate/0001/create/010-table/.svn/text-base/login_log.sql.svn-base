create table login_log (
    session_number int not null auto_increment,
    login_id varchar(255),
    login_timestamp timestamp,
    logout_timestamp timestamp,
    constraint login_log_PK primary key(session_number)
);
